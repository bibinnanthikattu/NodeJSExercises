import Crypto from 'crypto';

const Id = Crypto.randomUUID();
console.log(Id);

// In terminal too we can use REPL

// node : enter
// global. :double tab
// global.crypto : lists the functions inside this crypto module
// cyrpto.radnumUUID() : generates a random Id